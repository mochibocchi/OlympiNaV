<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.OlympiNav" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <!-- Set the background colour of action buttons: -->
        <item name="colorPrimary">@color/main_colour</item>
        <item name="colorPrimaryVariant">@color/main_colour</item>
        <!-- Set the colour of icon inside action buttons: -->
        <item name="colorOnPrimary">@color/white</item>

        <!-- Secondary brand color. -->
        <!-- Set the background colour of floating action buttons: -->
        <item name="colorSecondary">@color/main_colour</item>
        <item name="colorSecondaryVariant">@color/main_colour</item>
        <!-- Set the colour of icon inside floating action buttons: -->
        <item name="colorOnSecondary">@color/white</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
    </style>

    <style name="DialogAnimation">
        <item name="android:windowEnterAnimation">@anim/slide_in</item>
        <item name="android:windowExitAnimation">@anim/slide_out</item>
    </style>

    <!-- Base application theme. -->
    <style name="Base.Theme.OlympiNav" parent="Theme.Material3.DayNight.NoActionBar"></style>

    <style name="Divider">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">1dp</item>
        <item name="android:background">@color/black</item>
    </style>

    <style name="StandardEditText">
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:ems">10</item>
        <item name="android:singleLine">true</item>
        <item name="android:textCursorDrawable">@color/main_colour</item>
        <item name="android:backgroundTint">@color/main_colour</item>
        <item name="android:inputType">textNoSuggestions</item>
    </style>
</resources>